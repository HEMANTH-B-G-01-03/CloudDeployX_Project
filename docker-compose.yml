services:
  backend:
    build: ./backend
    container_name: clouddeployx-backend
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    container_name: clouddeployx-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend